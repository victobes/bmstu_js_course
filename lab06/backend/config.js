const postgresConfig = {
    host: 'localhost',
    port: 5432,
    user: 'victoria',
    password: '',
    db: 'animals',
};

const serverConfig = {
    host: 'localhost',
    port: 8010,
}

module.exports = {
    postgresConfig,
    serverConfig
};